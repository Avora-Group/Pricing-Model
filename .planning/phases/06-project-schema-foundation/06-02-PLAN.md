---
phase: 06-project-schema-foundation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - fastapi-project/migrations/009_link_quotes_to_projects.sql
  - fastapi-project/tests/conftest.py
  - fastapi-project/tests/test_quotes.py
autonomous: true
requirements: [PROJ-03]
must_haves:
  truths:
    - "Every quote can carry a nullable project_id linking it to a pricing project"
    - "Existing quotes remain valid with project_id = NULL (no quote is broken)"
    - "A quote with project_id = None still serializes through the existing create/list path"
    - "Migration 009 is idempotent (re-runnable with no error)"
  artifacts:
    - path: "fastapi-project/migrations/009_link_quotes_to_projects.sql"
      provides: "nullable quotes.project_id FK to pricing_projects(id) + index"
      contains: "ADD COLUMN IF NOT EXISTS project_id INTEGER REFERENCES pricing_projects(id)"
  key_links:
    - from: "fastapi-project/migrations/009_link_quotes_to_projects.sql"
      to: "pricing_projects(id)"
      via: "FK REFERENCES (nullable, NO ACTION on delete)"
      pattern: "REFERENCES pricing_projects\\(id\\)"
    - from: "fastapi-project/tests/conftest.py"
      to: "quotes mock rows"
      via: "_handle_quotes_insert setdefault project_id None"
      pattern: "setdefault\\(\"project_id\""
---

<objective>
Add a nullable `project_id` FK column to `quotes`, linking quotes to pricing projects. The COLUMN lands here (foundation); the WIRING that actually populates it through the save flow is Phase 7 (PROJ-03). Existing quotes get `project_id = NULL` (valid), and standalone project-less quotes remain possible forever.

Purpose: Phase 7's auto-sign and Phase 10's metrics both need quotes attributable to a project. The column must exist before any wiring can use it, and keeping all schema changes in this foundation phase avoids scattering migrations across behavior phases.
Output: Migration 009, a mock-DB forward-compat default, and a test proving a null-`project_id` quote still serializes.
</objective>

<execution_context>
@/Users/abu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-project-schema-foundation/06-RESEARCH.md

# Migration conventions to mirror
@fastapi-project/migrations/007_azure_auth.sql

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->

pricing_projects.id is the PK (from migrations/003) — the FK target already exists.

Mock DB _handle_quotes_insert (conftest.py:984) parses INSERT columns dynamically and setdefaults
status='draft' and created_at. The real create_quote INSERT is NOT modified in this phase (wiring is
Phase 7), so a project_id forward-compat setdefault is all that's needed for the mock to stay green.
Do NOT touch _handle_quotes_update (line 1020) — it only handles status changes and is unrelated.

NOTE: This plan edits tests/conftest.py, which 06-01 also edits — hence depends_on 06-01 (Wave 2)
to avoid a merge conflict on the same file. Apply this plan's conftest edit on top of 06-01's.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 009 (nullable quotes.project_id FK)</name>
  <files>fastapi-project/migrations/009_link_quotes_to_projects.sql</files>
  <action>
    Create `fastapi-project/migrations/009_link_quotes_to_projects.sql` following the 007 idempotent ALTER convention. Do NOT add BEGIN/COMMIT — the runner wraps each file in a transaction.

    Header comment in the 006/007 style:
    `-- Migration 009: Link quotes to projects via nullable FK`
    `-- Phase 6: Project Schema Foundation (foundation for PROJ-03; wiring lands in Phase 7)`
    Document: "project_id is NULLABLE — existing quotes stay NULL (valid); standalone quotes remain possible. ON DELETE defaults to NO ACTION so a linked quote blocks silent project deletion."

    Statements (keep the FK INLINE with ADD COLUMN IF NOT EXISTS so a re-run skips them together — do NOT split into a standalone ADD CONSTRAINT, which would not be idempotent):
    ```sql
    ALTER TABLE quotes
      ADD COLUMN IF NOT EXISTS project_id INTEGER REFERENCES pricing_projects(id);

    CREATE INDEX IF NOT EXISTS idx_quotes_project ON quotes(project_id);
    ```
    Do NOT make project_id NOT NULL. Do NOT add ON DELETE CASCADE. Do NOT write any backfill UPDATE.
  </action>
  <verify>
    <automated>cd fastapi-project && grep -q "ADD COLUMN IF NOT EXISTS project_id INTEGER REFERENCES pricing_projects(id)" migrations/009_link_quotes_to_projects.sql && grep -q "idx_quotes_project" migrations/009_link_quotes_to_projects.sql && ! grep -qi "NOT NULL" migrations/009_link_quotes_to_projects.sql && ! grep -qi "BEGIN;" migrations/009_link_quotes_to_projects.sql && echo MIGRATION_OK</automated>
  </verify>
  <done>File exists with a nullable inline-FK project_id column referencing pricing_projects(id), the index, no NOT NULL, no BEGIN/COMMIT, no backfill.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mock-DB forward-compat + test that null project_id quote serializes</name>
  <files>fastapi-project/tests/conftest.py, fastapi-project/tests/test_quotes.py</files>
  <behavior>
    - In conftest.py _handle_quotes_insert, a quote row without an explicit project_id defaults project_id=None.
    - Existing quote create/list/detail tests still pass (no regression).
    - New test: creating a quote via the existing POST /quotes/ path produces a quote that serializes successfully with project_id absent/None — proving a project-less quote remains valid.
  </behavior>
  <action>
    1. In `fastapi-project/tests/conftest.py`, in `_handle_quotes_insert` (~line 1011, the setdefault block where status='draft' is set), add:
       ```python
       new_row.setdefault("project_id", None)
       ```
       This is forward-compat: create_quote's INSERT is not modified this phase, so without this the mock simply omits the key (harmless), but setting it explicitly None matches the real nullable column and lets tests assert on it. Do NOT modify `_handle_quotes_update`.

    2. In `fastapi-project/tests/test_quotes.py`, add a new async test (e.g. `test_quote_project_id_nullable`) that creates a quote via the existing `_make_quote_payload()` + POST `/quotes/` flow (mirror `test_create_quote`, lines 63-76) and asserts the quote is created successfully (201) and that the stored/returned quote does not require a project_id — i.e. `data.get("project_id") in (None,)` or the key is simply absent, and the request still returns 201. This proves PROJ-03 foundation: a null linkage is valid and breaks nothing.
  </action>
  <verify>
    <automated>cd fastapi-project && python -m pytest tests/test_quotes.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>conftest setdefaults project_id=None on quote inserts; test_quotes.py has the new null-linkage test; all quote tests pass green.</done>
</task>

</tasks>

<verification>
- `cd fastapi-project && python -m pytest tests/ -q` — full suite green.
- Migration 009 adds a nullable inline-FK project_id referencing pricing_projects(id) + index, no NOT NULL, no BEGIN/COMMIT.
- Manual idempotency gate (Phase gate): run `psql $DATABASE_URL -f migrations/009_link_quotes_to_projects.sql` twice against a scratch Postgres — second run is a no-op with no error.
</verification>

<success_criteria>
- quotes can carry a nullable project_id FK to pricing_projects(id) (PROJ-03 foundation; wiring deferred to Phase 7).
- Existing quotes / project-less quotes remain valid (null linkage).
- Migration 009 is idempotent and follows the 007 convention.
- Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-project-schema-foundation/06-02-SUMMARY.md`
</output>
